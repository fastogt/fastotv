CMAKE_MINIMUM_REQUIRED(VERSION 3.3.0)
PROJECT(inih C)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_LIST_DIR}/inih)

SET(HEADERS_INIH
  inih/ini.h
)

SET(SOURCES_INIH
  inih/ini.c
)

SET(LIBS_INIH)

ADD_LIBRARY(${PROJECT_NAME} STATIC ${HEADERS_INIH} ${SOURCES_INIH})
